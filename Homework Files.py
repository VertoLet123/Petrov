from fileinput import close
Задача №1 (Раздел: Работа с файлами — чтение)
У тебя есть файл с именем "File from homework" (ты указал, что он лежит в папке с проектом).
Напиши код, который:
Открывает этот файл в режиме для чтения ('r')
Читает всё содержимое файла с помощью метода .read()
Сохраняет прочитанное в переменную content
Выводит content на экран
Закрывает файл (без использования with)
Важно: используй классический способ открытия и закрытия файла через open() и .close(),
без контекстного менеджера with. Контекстный менеджер будет в следующей задаче.
Подсказка:
Не забудь про режим открытия 'r' и про закрытие файла после чтения.
Напиши код. Я проверю.

file = open("File for homework", 'r', encoding='utf-8')
content = file.read()
print(content)
file.close()


Задача №2 (Раздел: Контекстный менеджер with)
У тебя есть файл "File for homework" (содержимое тебе уже знакомо).
Напиши код, который:
Использует контекстный менеджер with для открытия файла в режиме чтения ('r')
Читает содержимое файла методом .read()
Сохраняет прочитанное в переменную data
Выводит data на экран
Важно: Не используй .close() — контекстный менеджер закроет файл автоматически.

with open ("File for homework", 'r', encoding='utf-8') as file:
    data = file.read()
    print(data)


Задача №3 (Чтение всего файла без with)
Открой файл "File for homework" в режиме чтения, прочитай его целиком с помощью .read()
и выведи содержимое на экран. Закрой файл вручную. Не используй with.

file = open("File for homework", 'r', encoding='utf-8')
print(file.read())
file.close()


Задача №4 (Чтение всего файла с with)
Сделай то же самое, что в задаче №1 (прочитать файл "File for homework" и вывести содержимое), но используя контекстный менеджер with.
Без ручного закрытия файла.

with open ("File for homework", 'r', encoding='utf-8') as file:
    print(file.read())


Задача №5 (Запись в новый файл)
Создай (или перезапиши) файл с именем "output.txt". Запиши в него ровно одну строку:
"Первая запись в файл"
Используй with и режим 'w'.

with open ("output.txt", 'w', encoding='utf-8') as file:
    file.write("Первая запись в файл")


Задача №6 (Дозапись в файл)
Открой файл "output.txt" в режиме дозаписи ('a'). Добавь в конец файла новую строку:
"Вторая строка после дозаписи"
Убедись (мысленно), что первая строка из задачи №3 осталась на месте.
Используй with.

with open ("output.txt", 'a', encoding='utf-8') as file:
    file.write("Вторая строка после дозаписи")


Задача №7 (Проблема с курсором и seek)
Открой файл "output.txt" в режиме чтения 'r' (с with). Выполни следующие действия по порядку:
Прочитай первые 5 символов с помощью .read(5) и выведи их на экран.
С помощью метода .seek(0) перемести курсор в начало файла.
Прочитай всё содержимое файла с помощью .read() и выведи его на экран.
Подумай (вслух или про себя):
Почему без .seek(0) второе чтение вернуло бы пустую строку или неполный результат?

with open ("output.txt", 'r', encoding='utf-8') as file:
    print(file.read(5))
    file.seek(0)
    print(file.read())


Задача №8 (Чтение построчно)
Открой файл "File for homework" с помощью with в режиме чтения 'r'.
Прочитай и выведи на экран все строки по одной с помощью цикла for line in file:.
Каждую строку выводи с её номером (начиная с 1).
Пример вывода (для твоего файла):
text
1: Привет!
2: Я прохожу курс по автоматизации тестирования на Phyton
...
Подсказка (без готового кода):
Строки в файле можно перебирать прямо в цикле for.
Номер строки можно считать отдельной переменной, увеличивая её на каждой итерации.
Не забудь про .strip() или end='', чтобы избежать лишних пустых строк, но это не строгое требование.
Напиши код. Я проверю.


with open ("File for homework", 'r', encoding='utf-8') as file:
    line_number = 1
    for line in file:
        print(line_number, line.strip())
        line_number += 1


Задача №9 (Режим r+ — чтение и запись)
Открой файл "output.txt" (который ты создал в задачах 3-4) в режиме 'r+' (чтение и запись без удаления содержимого).
Выполни следующие действия по порядку:
Прочитай первые 10 символов и выведи их на экран.
Перемести курсор в начало файла с помощью .seek(0).
Запиши в начало файла строку "START " (обрати внимание: в Python строки нельзя просто "вставить"
в начало — запись перезапишет существующие символы на позиции курсора).
Закрой файл.
После выполнения открой файл "output.txt" в любом текстовом редакторе (или прочитай кодом) и посмотри, что получилось.
Не переживай, если начало файла изменилось неожиданно — это особенность режима 'r+'.
Важно:
Режим 'r+' не усекает файл, но запись поверх существующих данных, а не вставка.
Используй with.

with open ("output.txt",'r+', encoding='utf-8') as file:
    print(file.read(10))
    file.seek(0)
    file.write("START ")


Задача №10 (Работа с бинарным режимом 'b')
Создай файл "data.bin" в режиме 'wb' (запись байтов). Запиши в него байтовую строку: b'Hello\x00World'.
Затем открой этот же файл в режиме 'rb' (чтение байтов), прочитай всё содержимое и выведи его на экран.
Обрати внимание: \x00 — это нулевой байт, он не должен сломать вывод.
Используй with для обоих действий.
Подсказки без кода:
Бинарный режим означает, что ты работаешь с байтами, а не с текстом. Поэтому не указывай encoding='utf-8'.
При записи в 'wb' файл создастся или перезапишется.
При чтении в 'rb' метод .read() вернёт байтовую строку (тип bytes).
Вывести байтовую строку можно напрямую через print(). Она покажет b'Hello\x00World'.


with open ("data.bin", 'wb') as file:
    file.write(b'Hello\x00World')
with open ("data.bin", 'rb') as file:
    print(file.read())
