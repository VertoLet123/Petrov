from curses import wrapper
from operator import ifloordiv
from xml.dom.minidom import ProcessingInstruction

Задача №1 (Раздел: Функции в Python – создание и вызов)
Напиши функцию greet(), которая:
не принимает никаких аргументов
выводит на экран строку "Привет, автоматизация!"
После определения функции вызови её (чтобы увидеть вывод).
Используй ключевое слово def.
Напиши код. Я проверю.

def greet():
    print("Привет, автоматизация!")

greet()


Задача №2 (Раздел: Аргументы функций)
Напиши функцию greet_user(name), которая:
принимает один аргумент — имя пользователя
выводит на экран приветствие: "Привет, {name}!" (где {name} — подставленное имя)
После определения функции вызови её дважды:
с именем "Анна"
с именем "Иван"
Подсказка:
Внутри функции используй f-строку для подстановки имени: f"Привет, {name}!"


def greet_user(name):
    print(f"Привет, {name}!")

greet_user("Анна")
greet_user("Иван")


Задача №3 (Раздел: Аргументы с дефолтными значениями)
Напиши функцию greet_user_default(name), которая:
принимает один аргумент name
имеет дефолтное значение "Гость"
Функция должна выводить на экран:
"Привет, {name}!"
После определения функции:
Вызови её без аргумента (должно вывести "Привет, Гость!")
Вызови её с аргументом "Максим"
Направление (без готового кода):
Дефолтное значение задаётся через = в списке аргументов: def func(name="Гость").


def greet_user(name="Гость"):
    print(f"Привет, {name}!")

greet_user()
greet_user("Максим")


Задача №4 (Раздел: Возвращение данных из функций)
Напиши функцию square(num), которая:
принимает один аргумент — число
возвращает квадрат этого числа (число умноженное на само себя)
ничего не выводит на экран внутри функции
После определения функции:
Вызови функцию с аргументом 5 и сохрани результат в переменную result
Выведи result на экран
Направление (без готового кода):
Для возврата значения используется оператор #return
Внутри функции не используй print()


def square(num):
    return num ** 2

result = square(5)
print(result)


Задача №5 (Раздел: Декораторы)
Напиши декоратор timer, который:
ничего не измеряет (это учебный пример)
просто выводит на экран строку "Начинаем..." перед вызовом функции
и строку "Заканчиваем!" после вызова функции
Затем:
Примени декоратор timer к функции greet(), которая выводит "Привет!"
Вызови функцию greet()
Направление (без готового кода):
Декоратор — это функция, которая принимает другую функцию как аргумент
Внутри она определяет "обёртку" (wrapper), которая добавляет действия до и после вызова исходной функции
Синтаксис применения декоратора: @timer перед определением функции
Попробуй написать. Если сложно — попроси подсказку.


def timer(func):
    def wrapper():
        print("Начинаем...")
        func()
        print("Заканчиваем!")
    return wrapper

@timer
def greet():
    print("Привет!")

greet()


2. Простая задача на декораторы
Задача №6 (Раздел: Декораторы — закрепление)
Напиши декоратор say_hello, который:
перед вызовом функции выводит "Здравствуйте!"
после вызова функции выводит "До свидания!"
Затем:
Примени декоратор say_hello к функции show_name(), которая выводит "Меня зовут Алексей"
Вызови show_name()
Ожидаемый вывод:
Здравствуйте!
Меня зовут Алексей
До свидания!

def say_hello(func):
    def wrapper():
        print("Здравствуйте!")
        func()
        print("До свидания!")
    return wrapper

@say_hello
def show_name():
    print("Меня зовут Алексей")
show_name()


Напиши функцию say_python(), которая выводит на экран строку "Python — это сила!".
Вызови её.

def say_python():
    print("Python — это сила!")
say_python()


Задача №8 (Повторение: аргументы функции)
Напиши функцию multiply(a, b), которая:
принимает два аргумента
возвращает их произведение
внутри функции ничего не выводит на экран
После определения:
Вызови функцию с аргументами 4 и 5
Сохрани результат в переменную result
Выведи result на экран

def multiply (a, b):
    return a * b

result = multiply(4, 5)
print(result)


Задача №9 (Повторение: аргументы с дефолтными значениями)
Напиши функцию greet_with_title(name, title="Уважаемый"), которая:
принимает обязательный параметр name
принимает параметр title с дефолтным значением "Уважаемый"
выводит на экран строку: "{title} {name}, здравствуйте!"
После определения:
Вызови функцию с одним аргументом "Иван" (должно вывести "Уважаемый Иван, здравствуйте!")
Вызови функцию с двумя аргументами: "Мария" и "Уважаемая" (должно вывести "Уважаемая Мария, здравствуйте!")
Направление:
Внутри функции используй f-строку. Для вывода используй print().

def greet_with_title (name, title="Уважаемый"):
    print(f"{title} {name}, здравствуйте!")

greet_with_title(name="Иван")
greet_with_title(name="Мария", title="Уважаемая")


Задача №10 (Повторение: возврат значения + использование результата)
Напиши функцию add_and_double(a, b), которая:
принимает два числа
возвращает их сумму, умноженную на 2 (формула: (a + b) * 2)
внутри функции ничего не выводит
После определения:
Вызови функцию с аргументами 3 и 7 и сохрани результат в переменную computed
Выведи на экран фразу: "Результат: X", где X — значение computed

def add_and_double(a,b):
    return (a + b) * 2

computed = add_and_double(3,7)
print(f"Результат: {computed}")

Задача №11 (Повторение: функция с условием)
Напиши функцию is_even(n), которая:
принимает одно число n
возвращает True, если число чётное (n % 2 == 0)
возвращает False, если число нечётное
ничего не выводит на экран
После определения:
Вызови функцию с аргументом 8
Сохрани результат в переменную check1
Вызови функцию с аргументом 9
Сохрани результат в переменную check2
Выведи на экран обе переменные в одной строке через пробел
Направление:
Внутри функции можно написать #return n % 2 == 0 — это само вернёт True или False.

def is_even(n):
    return n % 2 == 0

check1 = is_even(8)
check2= is_even(9)
print(check1, check2)


Задача №12 (Повторение: функция со списком)
Напиши функцию get_long_words(words, min_length), которая:
принимает список строк words
принимает число min_length
возвращает новый список, содержащий только те слова, длина которых больше или равна min_length
ничего не выводит на экран
После определения:
Создай список: words = ["кот", "автоматизация", "python", "мир", "декоратор"]
Вызови функцию с words и min_length=5, сохрани результат в long_words
Выведи long_words на экран
Ожидаемый вывод:
['автоматизация', 'python', 'декоратор']
Направление:
Внутри функции создай пустой список, затем циклом пройди по words,
проверяй len(word) >= min_length и добавляй подходящие в новый список. В конце верни этот список.

def get_long_words(words, min_length):
    result = []
    for word in words:
        if len(word) >= min_length:
            result.append(word)
    return result

words = ["кот", "автоматизация", "python", "мир", "декоратор"]
long_words = get_long_words(words, min_length=5)
print(long_words)


Задача №13 (Декоратор — закрепление)
Напиши декоратор uppercase_result, который:
преобразует результат декорируемой функции в строку (если результат не строка — примени str())
возвращает эту строку в верхнем регистре (метод .upper())
Затем:
Примени декоратор uppercase_result к функции get_greeting(), которая возвращает строку "Привет, мир!"
Вызови функцию get_greeting() и сохрани результат в переменную message
Выведи message на экран
Ожидаемый вывод:
"ПРИВЕТ, МИР!"
Направление (без готового кода):
Декоратор uppercase_result(func) — принимает функцию
Внутри создаёт wrapper()
В wrapper() вызывает func(), сохраняет результат
Применяет str() и .upper() к результату
Возвращает преобразованный результат
Декоратор возвращает wrapper
Применение: @uppercase_result перед def get_greeting():
Вызов: get_greeting()

def uppercase_result(func):
    def wrapper():
        result = func()
        result = str(result).upper()
        return result
    return wrapper

@uppercase_result
def get_greeting():
    return "Привет, мир!"
message = get_greeting()
print(message)

#Декоратор uppercase_result(func) определён ✅
#wrapper() внутри вызывает func() ✅
#result = str(result).upper() — преобразует результат в строку и в верхний регистр ✅
#return result возвращает преобразованное значение ✅
#return wrapper — декоратор возвращает обёртку ✅
#@uppercase_result применён к get_greeting() ✅
#get_greeting() возвращает "Привет, мир!" ✅
#message = get_greeting() → message = "ПРИВЕТ, МИР!" ✅
#print(message) → выводит ПРИВЕТ, МИР! ✅


Задача №14 (Декоратор — последний)
Напиши декоратор repeat_twice, который:
вызывает декорируемую функцию два раза подряд
ничего не возвращает (но если функция что-то возвращает — просто игнорируем)
Затем:
Примени декоратор repeat_twice к функции say_hello(), которая выводит на экран "Hello!"
Вызови функцию say_hello() один раз
Ожидаемый вывод:
text
Hello!
Hello!
Направление (без готового кода):
Декоратор принимает функцию (func)
Внутри создаёт wrapper()
В wrapper() вызови func() два раза (просто напиши func() дважды или через цикл)
#return wrapper


def repeat_twice(func):
    def wrapper():
        func()
        func()
    return wrapper

@repeat_twice
def say_hello():
    print("Hello!")
say_hello()

