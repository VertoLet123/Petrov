Задача №1 (Раздел: Создание класса и объекта класса)
Создай класс Car. У класса пока не должно быть ни конструктора, ни методов, ни атрибутов — просто пустой класс.
Затем:
Создай объект (экземпляр) этого класса и сохрани его в переменную my_car
Выведи на экран тип переменной my_car (функция type())
Подсказка (без готового кода):
Класс создаётся с помощью ключевого слова class
Объект создаётся через имя класса со скобками: ИмяКласса()

class Car:
    pass

my_car = Car()
print(type(my_car))



Задача №2 (Раздел: Конструктор класса __init__)
Создай класс Dog с конструктором __init__, который:
принимает два параметра: name и age
сохраняет их в атрибуты объекта: self.name и self.age
Затем:
Создай объект класса Dog с именем "Шарик" и возрастом 3
Выведи на экран атрибуты этого объекта: сначала name, потом age (в одной строке через пробел)
Направление (без готового кода):
Конструктор — это метод __init__
Его первый параметр всегда self
Дальше идут параметры, которые передаются при создании объекта
Внутри конструктора: self.name = name и self.age = age

class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

My_dog = Dog ("Шарик" , 3)
print(My_dog.name, My_dog.age)


Задача №3 (Раздел: Методы класса)
Создай класс Calculator, у которого:
Метод add(self, a, b), который возвращает сумму a и b
Метод multiply(self, a, b), который возвращает произведение a и b
Никакого конструктора __init__ в этом классе не нужно.
Затем:
Создай объект класса Calculator и сохрани его в переменную calc
Вызови метод add с аргументами 5 и 3, сохрани результат в sum_result
Вызови метод multiply с аргументами 4 и 2, сохрани результат в mul_result
Выведи на экран оба результата в одной строке через пробел
Направление (без готового кода):
Методы внутри класса всегда первым параметром принимают self
Дальше идут нужные параметры
Внутри метода используй #return
Вызов метода: calc.add(5, 3)


class Calculator:
    def add(self, a, b):
        return a+b
    def multiply(self, a, b):
        return a*b

calc = Calculator()
sum_result = calc.add(5, 3)
mul_result = calc.multiply(4, 2)
print (sum_result, mul_result)


Задача №4 (Раздел: Общие атрибуты / атрибуты класса)
Создай класс Animal с атрибутом класса species = "млекопитающее" (это общий для всех объектов атрибут).
Затем:
Создай два объекта класса Animal: animal1 и animal2
Выведи на экран атрибут класса species через первый объект
Выведи на экран атрибут класса species через второй объект
Выведи на экран атрибут класса species напрямую через сам класс
Направление (без готового кода):
Атрибут класса создаётся внутри класса, но вне всех методов (просто в теле класса)
Доступ через объект: animal1.species
Доступ через класс: Animal.species
Напиши код. Я проверю.

class Animal:
    species = "млекопитающее"

animal1.species
animal2.species
Animal.species
print(animal1.species, animal2.species, Animal.species)


Задача №5 (Раздел: Понимание параметра self)
Создай класс Person с конструктором __init__, который:
принимает параметры name и age
сохраняет их в атрибуты объекта self.name и self.age
Затем создай метод introduce(self), который:
выводит на экран фразу: "Меня зовут {name}, мне {age} лет" (используя атрибуты объекта)
После определения класса:
Создай объект p1 с именем "Анна" и возрастом 25
Вызови метод introduce() у этого объекта
Направление (без готового кода):
В методе introduce не нужно принимать ничего кроме self
Внутри метода обращайся к self.name и self.age
Используй f-строку для вывода


class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    def introduce(self):
        print(f"Меня зовут {self.name}, мне {self.age} лет")

p1 = Person ("Анна", 25)
p1.introduce()


Задача №6 (Раздел: Введение в ООП — практика)
Создай класс Book с:
Конструктором __init__, который принимает параметры title и author и сохраняет их в атрибуты self.title и self.author
Атрибутом класса material = "бумага"
Методом info(self), который возвращает строку:
"Книга: {title}, Автор: {author}, Материал: {material}"
(где material — атрибут класса)
Затем:
Создай объект book1 с названием "Война и мир" и автором "Толстой"
Создай объект book2 с названием "Преступление и наказание" и автором "Достоевский"
Для каждого объекта вызови метод info() и выведи результат на экран
Направление (без готового кода):
Атрибут класса material создаётся вне методов, прямо внутри класса
В методе info для доступа к атрибуту класса используй self.material или Book.material
Метод info должен возвращать строку, а не печатать её


class Book_c:
    material = "бумага"
    def __init__(self, title, author):
        self.title = title
        self.author = author

    def info(self):
        return f"Книга: {self.title}, Автор: {self.author}, Материал: {Book_c.material}"

book1 = Book_c ("Война и мир", "Толстой")
book2 = Book_c ("Преступление и наказание", "Достоевский")

book1.info()
book2.info()

print(book1.info(), book2.info())